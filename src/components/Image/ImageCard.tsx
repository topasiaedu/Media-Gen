/**
 * ImageCard Component
 * Displays individual image card with actions
 */

import React from "react";

const ImageCard: React.FC = () => {
  return (
    <div>
      {/* TODO: Implement ImageCard */}
    </div>
  );
};

export default ImageCard; 